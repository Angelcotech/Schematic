{
  "name": "schematic",
  "version": "0.0.1",
  "private": true,
  "description": "Live architecture map — reference surface for Claude Code",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @schematic/frontend dev",
    "typecheck": "pnpm -r --parallel typecheck"
  }
}
