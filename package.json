{
  "name": "schematic-workspace",
  "version": "0.0.1",
  "private": true,
  "description": "Schematic — monorepo root (publishable package lives in app/)",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "rm -rf app/dist frontend/dist",
    "build": "pnpm clean && pnpm --filter claude-schematic build && pnpm --filter @schematic/frontend build && node scripts/copy-frontend.mjs",
    "dev": "pnpm --filter @schematic/frontend dev",
    "typecheck": "pnpm -r --parallel typecheck",
    "pack:app": "pnpm clean && pnpm build && pnpm --filter claude-schematic pack"
  }
}
